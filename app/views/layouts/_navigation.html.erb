<a class=" item" href="/">
  Chatroom
</a>
<a class="item">
  Messages
</a>
<div class="right menu">
  <div class="ui dropdown item">
    Account <i class="dropdown icon"></i>
    <div class="menu">
<!--      <a class="item" ><%#=link_to "Login" ,login_path %></a>-->
      <% if user_signed_in? %>
           <a class="item" data-method="delete" href="/logout" >Log out</a>
        <a class="item"  href="/edit" >Edit Account</a>
      <%else%>
          <a class="item" href=/login>Log in</a>
          <a class="item" href="/sign_up">SignUp</a>
      <%end %>
    </div>
  </div>

  <div class="item">
    <% if user_signed_in? %>
      <div class="middle aligned content">
        <div class="header">
          <i class="like icon"></i>
          <%=current_user.name %>
        </div>
      </div>
<!--    <p><%#=current_user.name %></p>-->
      <% else %>
<!--    <div class="ui primary button">Sign Up</div>-->
      <%#= button_to  sign_up, class: "ui primary button", method: :get %>
      <div class="ui button" data-role="button" ><%= link_to 'SignUp', new_user_registration_path  %></div>
      <%end %>
  </div>
</div>